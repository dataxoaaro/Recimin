-- Web push is removed. Imports finish fast enough that the phone is still in
-- hand when they land — a notification arriving seconds after the share sheet
-- closed told nobody anything. The Imports screen shows the outcome.
DROP TABLE push_subscriptions;
